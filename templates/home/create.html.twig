{% extends 'base.html.twig' %}

{% block title %}add Movies Series{% endblock %}

{% block body %}
    <div class="container">
        <div class="card">
            <h1>Ajout d'un film ou serie</h1>
            {{ form_start(form) }}
            {{ form_end(form) }}
            <p style="color: red">{{ errors }}</p>
        </div>
    </div>
{% endblock %}
